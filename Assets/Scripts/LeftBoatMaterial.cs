using System.Collections.Generic;
using System.Collections;
using UnityEngine;


public class LeftBoatMaterial : MonoBehaviour {
    /*
    public Material[] boatMaterials;
    public Material[] darkerBoatMaterials;

    Transform boatTransform;
    GameObject hull;
    GameObject[] sits = new GameObject[2];

    GameObject leftChoiceGO;
    ChosenColor cC;
    
    Material[] newMaterials = new Material[2];

    void Start() {
        leftChoiceGO = GameObject.Find("LeftSide");
        cC = leftChoiceGO.GetComponent<ChosenColor>();



        newMaterials[0] = boatMaterials[cC.chosenColorIndex];
        newMaterials[1] = boatMaterials[cC.chosenColorIndex];

        boatTransform = gameObject.transform.GetChild(1);
        hull = boatTransform.transform.Find("Kad³ub").gameObject;
        sits[0] = boatTransform.transform.GetChild(1).gameObject;
        sits[1] = boatTransform.transform.GetChild(2).gameObject;

        ChangeHullColor();
        ChangeSitsColor();
    }

    private void ChangeHullColor() {
        hull.GetComponent<MeshRenderer>().materials = newMaterials;
    }

    private void ChangeSitsColor() {
        foreach (GameObject sit in sits) {
            sit.GetComponent<MeshRenderer>().material = darkerBoatMaterials[cC.chosenColorIndex];
        }
    }
    */










}